name: Deploy Fake
on:
  workflow_call:
jobs:
  deploy-kubernetes:
    runs-on: ubuntu-latest 
    steps:
      - name: Deploy app-a
        run: echo "Executando deploy mo ambiente de desenvolvimento"
