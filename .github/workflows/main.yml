name: CI/CD Fake
on: 
    workflow_dispatch:  
      inputs:
        ambiente:
        type: choice
        default: "desenvolvimento"
        required: true
        description: " Ambiete à ser deployado a app-a"
        options:
          - desenvolvimento
          - homologação
          - produção

jobs:
    CI:
        runs-on: ubuntu-latest
        steps:
            - name: Build de Imagem Docker
              run: echo "Execução do comando de docker build"
            - name: Execução de Testes de Software
              run: echo "Aqui eu executo o teste de software"
            - name: Publicando a Release
              run: echo "Publicação da Relase final"
     
    CD:
      needs: [CI]
      uses: deniscsilva/workflow_reaproveitavel/.github/workflows/cd.yaml@main

